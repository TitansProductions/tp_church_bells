Config = {}

-- Displays debugging prints.
Config.Debug = false 

-- Church locations.
Config.Locations = {

    ['VALENTINE'] = {

        Coords = vector3(-229.571, 804.2813, 125.03),

        -- The minimum distance to start playing the sound for nearby players.
        MaximumDistance = 350.0,

        StartVolume = 0.5,
        SoundSource = "church-bell", -- To add sounds go to interact-sound/client/html/sounds (MUST BE .ogg file)

        -- The in-game time to start playing the sound.
        StartHours = { 6, 18 }, -- in-game time.

        -- The time to repeat the sound.
        RepeatEvery = 8, -- time in real seconds.

        -- How long should the sound keep playing until it stops.
        TimeLast  = 1, -- time in minutes (real-time)

        
    },

    ['BLACKWATER'] = {

        Coords = vector3(-974.106, -1187.49, 59.005),

        -- The minimum distance to start playing the sound for nearby players.
        MaximumDistance = 350.0,

        StartVolume = 0.5,
        SoundSource = "church-bell", -- To add sounds go to interact-sound/client/html/sounds (MUST BE .ogg file)

        -- The in-game time to start playing the sound.
        StartHours = { 6, 18 }, -- in-game time.

        -- The time to repeat the sound.
        RepeatEvery = 8, -- time in real seconds.

        -- How long should the sound keep playing until it stops.
        TimeLast  = 1, -- time in minutes (real-time)

        
    },

    ['SAINT_DENIS'] = {

        Coords = vector3(2749.303, -1268.90, 52.131),

        -- The minimum distance to start playing the sound for nearby players.
        MaximumDistance = 350.0,

        StartVolume = 0.5,
        SoundSource = "church-bell", -- To add sounds go to interact-sound/client/html/sounds (MUST BE .ogg file)

        -- The in-game time to start playing the sound.
        StartHours = { 6, 18 }, -- in-game time.

        -- The time to repeat the sound.
        RepeatEvery = 8, -- time in real seconds.

        -- How long should the sound keep playing until it stops.
        TimeLast  = 1, -- time in minutes (real-time)

        
    },

    ['SAINT_DENIS'] = {

        Coords = vector3(2749.303, -1268.90, 52.131),

        -- The minimum distance to start playing the sound for nearby players.
        MaximumDistance = 350.0,

        StartVolume = 0.5,
        SoundSource = "church-bell", -- To add sounds go to interact-sound/client/html/sounds (MUST BE .ogg file)

        -- The in-game time to start playing the sound.
        StartHours = { 6, 18 }, -- in-game time.

        -- The time to repeat the sound.
        RepeatEvery = 8, -- time in real seconds.

        -- How long should the sound keep playing until it stops.
        TimeLast  = 1, -- time in minutes (real-time)

        
    },

    ['RHODES'] = {

        Coords = vector3(1287.713, -1219.85, 82.882),

        -- The minimum distance to start playing the sound for nearby players.
        MaximumDistance = 350.0,

        StartVolume = 0.5,
        SoundSource = "church-bell", -- To add sounds go to interact-sound/client/html/sounds (MUST BE .ogg file)

        -- The in-game time to start playing the sound.
        StartHours = { 6, 18 }, -- in-game time.

        -- The time to repeat the sound.
        RepeatEvery = 8, -- time in real seconds.

        -- How long should the sound keep playing until it stops.
        TimeLast  = 1, -- time in minutes (real-time)

        
    },

}